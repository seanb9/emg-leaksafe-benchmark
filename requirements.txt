# ReAble ml-v2 — Python dependencies
#
# Two install profiles (see README.md):
#   * DEV (this Mac, darwin/arm64): CPU/MPS PyTorch, `pip install torch`
#   * RIG (Windows, RTX 4080 Super / Ada sm_89): CUDA 12.x build, e.g.
#       pip install torch --index-url https://download.pytorch.org/whl/cu124
#
# Exact pinned versions are recorded in README.md once confirmed on each machine.
# This file lists the data/eval/test stack that step 1 (data + splits + tests) needs.

numpy>=1.26          # allow 2.x — required for Python 3.13 (1.26 has no 3.13 wheel)
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
PyYAML>=6.0
tqdm>=4.66
mat73>=0.65          # for any HDF5/v7.3 .mat (raw DB2 E1/E2/E3 are v5 → scipy handles them)
matplotlib>=3.7
pytest>=7.4

# Model/training stack.
# IMPORTANT: do NOT list torch/torchvision here. If they are in this file, a plain
# `pip install -r requirements.txt` pulls the CPU build from PyPI, which then
# shadows the CUDA build (pip reports "already satisfied" and skips it) -> you get
# "Torch not compiled with CUDA enabled". Install them separately, per machine:
#   RIG (CUDA): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   DEV (mac):  pip install torch torchvision
tensorboard>=2.15
thop>=0.1           # FLOP counting for the deployability table
